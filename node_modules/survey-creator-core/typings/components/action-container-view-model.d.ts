import { AdaptiveActionContainer, ResponsivityManager } from "survey-core";
declare class SurveyElementResponsivityManager extends ResponsivityManager {
    constructor(container: HTMLDivElement, model: SurveyElementActionContainer);
    shouldProcessResponsiveness(): boolean;
}
export declare class SurveyElementActionContainer extends AdaptiveActionContainer {
    alwaysShrink: boolean;
    protected responsivityManager: SurveyElementResponsivityManager;
    private needToShrink;
    private setModeForActions;
    private calcItemSize;
    constructor();
    fit(options: {
        availableSpace: number;
        gap?: number;
    }): void;
    private _isResponsivenessAllowed;
    allowResponsiveness(): void;
    get isResponsivenessAllowed(): boolean;
    protected createResponsivityManager(container: HTMLDivElement): ResponsivityManager;
}
export {};
